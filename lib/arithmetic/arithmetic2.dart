/// Returns whether three values can form a triangle
///
/// i.e. 3, 4, 5 => true, 1, 2, 4 => false
/// HINT: Pythagorean theorem
bool isTriangle(int a, int b, int c) => null;

/// Returns the product of all numbers from 1 to n
int product(int n) => null;

/// Calculates the sum of an arithmetic progression
/// SUM = A + (A + B) + (A + 2B) + (A + 3B) + ...
///
/// i.e. sequence starting with 5, increasing by 2 each time and sum 3 elements => 5 + 7 + 9 = 21
/// i.e. sequence starting with 3, increasing by 0 each time and sum 10 elements => 3 + 3 + ... + 3 = 30
int arithmeticProgression(int start, int increment, int sumCount) => null;

/// Returns a list of [count] Fibonacci numbers
/// The first two Fibonacci numbers are 1 and 1
/// Thereafter the n+1 number is n-1 + n number
///
/// i.e. 1, 1, 1+1=2, 1+2=3, 2+3=5, ...
List<int> fibonacciNumbers(int count) => null;
