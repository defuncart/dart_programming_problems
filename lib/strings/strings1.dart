/// Contains the number of vowels [a, e, i, o, u] in a string
///
/// Note that this should be case insensitive
int vowelCount(String string) => null;

/// Returns whether a string is identical forwards and backwards
///
/// Note that this should be case insensitive, while punctuation should be ignored
bool isPalindrome(String string) => null;
